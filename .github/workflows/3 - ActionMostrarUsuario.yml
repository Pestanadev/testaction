name: Comentar usuário do push

on:
  push:
    branches:
      - main

permissions:
  contents: write  # necessário para criar comentários em commits via GITHUB_TOKEN

jobs:
  comment-user:
    runs-on: ubuntu-latest
    steps:
      - name: Fazer checkout do código # necessário para ações que interagem com o repositório
        uses: actions/checkout@v4 # fazer checkout do código

      - name: Comentar no commit quem fez o push # comentar no commit quem fez o push
        uses: actions/github-script@v6 # usar ação para comentar
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }} # token para autenticação 
          script: |
            const actor = context.actor;
            const sha = context.sha;
            const body = `👋 O push foi feito por **${actor}**.`; 
            });
        
