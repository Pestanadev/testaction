name: Comentar usuário do push

on:
  push:
    branches:
      - main

permissions:
  contents: write  # necessário para criar comentários em commits via GITHUB_TOKEN

jobs:
  comment-user:
    runs-on: ubuntu-latest
    steps:
      - name: Fazer checkout do código
        uses: actions/checkout@v4

      - name: Comentar no commit quem fez o push
        env:
          ACTOR: ${{ github.actor }}
        run: |
          BODY="👋 O push foi feito por **${ACTOR}**."
        
